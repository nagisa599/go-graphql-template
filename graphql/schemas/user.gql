type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  todos: [Todo!]!
}

extend type Query {
  getAllUsers: [User!]!
}

extend type Mutation {
  createUser(name: String!, email: String!, password: String!): User!
}
